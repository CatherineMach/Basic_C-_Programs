using System;

class Program
{
    static void Main(string[] args)
    {
        int[] numList = { 32, 72, 98, 48, 50 };
        Console.WriteLine("Write a number to divide some numbers with:)");
        int userNum = Convert.ToInt32(Console.ReadLine());
        Console.WriteLine(numList[] + " divided by " + userNum + " equals... ");

        if (userNum < 0 || userNum >= numList.Length)
        {
            Console.WriteLine();
        }





        Console.ReadLine();
    }
    
}